// This file is autogenerated. Do not change its contents, it will be overwritten.

$schemes: (
        "inherit": (
        {% for prop in props  %}
                "{{ prop }}": inherit,
        {% endfor %}
        ),
  {%  for scheme in schemes %}
        "{{scheme.alias}}": (
        {% for prop in props  %}
                "{{ prop }}": {% if scheme[prop] ?? 0 %}{{ '#{$' }}{{ getColorVar(scheme[prop]) }}{{ '}' }}{% else %} inherit {% endif %},
        {% endfor %}
        ),
  {% endfor %}
);

@mixin scheme($scheme: 'inherit') {
  {% for prop in props  %}
    --scheme-color-{{ prop }}: #{map-get($schemes, $scheme, '{{ prop }}')};
  {% endfor %}
}

{% for layout, scheme in layoutSchemes %}
:root{
  @include scheme({{ scheme }});
}
{% endfor %}

@each $scheme in map-keys($schemes) {
  [data-scheme="#{$scheme}"] {
    @include scheme($scheme);
  }
}